void Sys_Error(const char *error, ...) {
    // This is a null implementation of Sys_Error to allow the tests to link.
    // The real implementation is in the engine and is not needed for unit tests.
}
